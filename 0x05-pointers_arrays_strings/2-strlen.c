#include "main.h"

/**
  * _strlen - returns the length of a string.
  * @s: string itself
  * Return: len (success)
  */

int _strlen(char *s)

{
	int *s = 0;

	while (*s != '\0')
	{
		len++;
		s++;
	}
	return (len);
}
